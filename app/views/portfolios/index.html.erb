<% for portfolio in @portfolios %>
  <div class="portfolio">
    <%= link_to image_tag(portfolio.rep_photo.image(:thumb)),  portfolio_path(portfolio) %>
    <%= link_to portfolio.name, portfolio_path(portfolio) %>
  </div>
<% end %>